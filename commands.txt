# Git commands for cloning and again pushing

1).git clone "url or ssh"                  (command)
2).Move to that folder
3).Now modify or add new files to that directory
5)git add.
4).git commit -m ("description")
5).git push

Commands for moving a project on your local machine to a github repository

1).git init
2).git status
3).git add
4).git commit -m ()
5).git remote add origin (url)
6).git push origin master


-->Git command for branch
1))Added to the feature branch